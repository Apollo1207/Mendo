import { StyleSheet } from 'react-native';
import { RestaurantCard } from '../../AppStyles';

const styles = StyleSheet.create({
    container: RestaurantCard.container,
    containerCalendar: RestaurantCard.containerCalendar,
    containerClock: RestaurantCard.containerClock,
    photo: RestaurantCard.photo,
    title: RestaurantCard.title,
    category_top: RestaurantCard.category_top,
    category_bottom: RestaurantCard.category_top,
    slidersIconClock: RestaurantCard.slidersIconClock,
    slidersIconCalendar: RestaurantCard.slidersIconCalendar,
});

export default styles;